-- Dashboard app migration v1 (ordering slot).
-- Runtime auth: ``users`` in ``003_platform_new_tables.sql``.
-- Legacy ``dashboard_upload`` (PDF JSON) removed; existing DBs: ``004_drop_dashboard_upload.sql``.

SELECT 1;
